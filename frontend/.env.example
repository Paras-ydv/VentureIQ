# Public URL of the VentureIQ API, including the /api prefix.
# Leave unset for local development: Vite proxies /api to 127.0.0.1:8000.
# On Vercel, set this in Project Settings -> Environment Variables.
VITE_API_BASE=https://your-backend-host/api
